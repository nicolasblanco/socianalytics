- page_title "Redirection #{@short_url.url}"

.data-table
  %table{:border => "0", :cellpadding => "0", :cellspacing => "0", :width => "100%"}
    %thead
      %tr
        %th{:colspan => "3"} Requêtes
    %tbody
      - @short_url.requests.each_with_index do |url_request, index|
        %tr{ table_tr_attrs(index) }
          %td= url_request.referrer
          %td= url_request.user_agent
          %td.date
            %strong= l url_request.created_at
